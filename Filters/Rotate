import Graphics.Image
import Graphics.Image.Processing
import Graphics.Image.IO

main :: IO ()
main = do
  frog <- readImageRGBA VU  "Cat03.jpg"
  writeImage "frog_eye_grid.png" $ rotate Bilinear (Fill 0) (11*pi/6) frog
